const SERVER_PORT = process.env.SERVER_PORT || '3001';
const SOCKETIO_PORT = process.env.SOCKETIO_PORT || '3002';
const SECRET = process.env.SECRET || 'VERY_SECRET_STRING';

export { SERVER_PORT, SOCKETIO_PORT, SECRET };
